//To run this program, you can save it as a .js file and then open it in a web browser.

const prompt = require('prompt-sync')();

//Create variables for the firs and last names
var fname = prompt("Enter your first name: ");
var lname = prompt("Enter your last name: ");

//Add the two names together and then output the result
console.log("Your name is, ", fname, lname);
